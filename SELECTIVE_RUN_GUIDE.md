# راهنمای اجرای انتخابی — فقط روش‌های جدید، بدون تکرار شبیه‌سازی‌های قبلی

**هدف:** افزودن روش‌های پیش‌بینی صریح (Persistence، Holt، ARYW، ARIMA) به مقایسه، طوری که **هیچ‌کدام از ۹ روش قبلی دوباره اجرا نشوند** و نتایج کش‌شده آن‌ها مستقیماً در جدول مقایسه بنشیند.

---

## خبر خوب: زیرساختش را خودتان قبلاً ساخته‌اید

موتور موازی فعلی (`parallel_engine.py`) دقیقاً مکانیزم لازم را دارد:

- هر روش که تمام می‌شود در `_status.json` پوشه اجرا علامت `done` می‌گیرد و فایل `protocol/<روش>_protocol.parquet` آن ذخیره می‌شود.
- با `--resume <پوشه>`، هر روشی که هم `done` باشد و هم فایل protocol داشته باشد **بی‌درنگ رد می‌شود** و فقط روش‌های غایب محاسبه می‌شوند.
- در پایان، `_merge` جدول `comparison/main_summary.csv` را از **همه** روش‌های done موجود در پوشه می‌سازد — کش قدیمی + محاسبه جدید، کنار هم.

پس فقط دو چیز کم بود که در این بسته اضافه شده: (۱) خود روش‌های جدید، (۲) یک اسکریپت کوچک که از اجرای قدیمی یک «پوشه مقایسه» بسازد تا **نتایج اصلی رساله دست‌نخورده بمانند**.

> ⚠️ چرا مستقیم روی پوشه اصلی resume نکنیم؟ چون `_merge` در پایان، `main_summary.csv` را فقط با روش‌هایی که در `--methods` داده‌اید بازنویسی می‌کند. اگر روی پوشه اصلی با `--methods WSPI ARIMA` resume کنید، جدول ۹ روشه اصلی‌تان با یک جدول ۲ روشه جایگزین می‌شود (فایل‌های protocol سالم می‌مانند ولی چرا ریسک؟). اسکریپت `prepare_compare_folder.py` این مشکل را کلاً حذف می‌کند.

---

## فایل‌های این بسته

| فایل | محل قرارگیری | نوع |
|---|---|---|
| `forecasting_baselines.py` | `methods\` | **جدید** — چهار روش پیش‌بینی صریح |
| `prepare_compare_folder.py` | `experiments\` | **جدید** — ساخت پوشه مقایسه از اجرای قبلی |
| `method_configs.py` | `evaluation\` | **جایگزین** — فقط گروه ۵ اضافه شده |
| `run_popularity_assessment.py` | `experiments\` | **جایگزین** — ثبت روش‌های جدید + opt-in بودنشان |

نکته مهم طراحی: روش‌های گروه ۵ **فقط وقتی اجرا می‌شوند که صریحاً در `--methods` نامشان را ببرید**. دستورهای قبلی شما بدون `--methods` همان رفتار همیشگی را دارند (۹ روش، بدون ARIMA). پس هیچ اجرای قدیمی‌ای خراب یا کند نمی‌شود.

## روش‌های جدید

| نام | ایده | سرعت هر (آیتم×پنجره) |
|---|---|---|
| `Persistence` | ساده‌لوح: افق بعدی = تکرار آخرین مشاهده (مبنای اجباری مقالات پیش‌بینی) | ~۰ |
| `Holt` | هموارسازی نمایی دوگانه (سطح + روند)، فرم بسته | ~۰ |
| `ARYW` | AR(2) با معادلات Yule-Walker (فرم بسته، بدون MLE) — عضو سریع خانواده ARMA | ~۰٫۱ms |
| `ARIMA` | ARMA(1,1) با MLE در پنجره لغزان — دقیقاً رویکرد مرجع [25] پروپوزال (Hassine et al.) | **~۲۴ms** |

امتیاز هر روش = مجموع مقادیر پیش‌بینی‌شده برای افق (`--horizon`؛ پیش‌فرض ۷ اسلات). چون حقیقت زمینی پروتکل شما تعاملات آینده است، همان چهار معیار همیشگی (NDCG/Spearman/RSI/ΔRank) این‌بار «کیفیت پیش‌بینی» را می‌سنجند — بدون هیچ تغییری در ارزیاب.

**پیش‌نیاز:** `pip install statsmodels` (اگر نصب نباشد، ARIMA به‌جای خطا با هشدار به ARYW برمی‌گردد).

---

## گردش کار (سه قدم)

### قدم ۱ — کپی فایل‌ها و تست یک‌دقیقه‌ای

فایل‌ها را در مسیرهایشان بگذارید، بعد:

```bat
python -c "from methods.forecasting_baselines import get_all_forecasting_methods; import numpy as np; [print(n, m.assess_single(np.arange(64.))) for n,m in get_all_forecasting_methods().items()]"
```

چهار عدد متناهی باید چاپ شود.

### قدم ۲ — ساخت پوشه مقایسه از اجرای کامل قبلی

```bat
python experiments\prepare_compare_folder.py results\youtube\main_<timestamp قبلی>
```

خروجی: پوشه `results\youtube\predcmp_<ts>` که فقط protocol و markerهای قبلی را دارد. **پوشه اصلی دست نمی‌خورد.** خود اسکریپت دستور قدم ۳ را هم چاپ می‌کند.

### قدم ۳ — اجرای فقط روش‌های جدید + ادغام با کش

```bat
python experiments\run_popularity_assessment.py youtube --cores 4 ^
    --resume results\youtube\predcmp_<ts> ^
    --methods WSPI AF DTCWT+AF Persistence Holt ARYW ARIMA
```

- `WSPI`، `AF`، `DTCWT+AF` → از کش، صفر ثانیه (در خروجی می‌بینید: `already done`).
- فقط چهار روش جدید واقعاً اجرا می‌شوند (هر کدام یک پروسه موازی).
- جدول نهایی: `results\youtube\predcmp_<ts>\comparison\main_summary.csv` شامل کش + جدید.

هر روش کش‌شده‌ای که بخواهید در جدول مقایسه باشد را در `--methods` نام ببرید (مثلاً اگر جدول کامل ۱۳ روشه می‌خواهید، هر ۱۳ نام را بدهید — باز هم فقط ۴ تای جدید محاسبه می‌شوند). اگر فقط «روش ما + جدیدها» را می‌خواهید، همان مثال بالا کافی است.

> ⚠️ **حتماً همان تنظیمات اجرای اصلی را تکرار کنید**: همان `--data-path` (برای سناریوهای تاکسی)، همان `--horizon`، `--window-size` و `--k-list`. وگرنه پنجره‌ها و آیتم‌های روش‌های جدید با کش قدیمی هم‌تراز نیستند و مقایسه ناعادلانه می‌شود. تنظیمات اصلی در `metadata\run_metadata.json` داخل پوشه کلون هست.

---

## برآورد زمان اجرا (فقط روش‌های جدید)

Persistence/Holt/ARYW در همه سناریوها عملاً رایگان‌اند (هم‌مرتبه با AF). گلوگاه فقط ARIMA است (~۲۴ms هر برازش، پنجره‌ها داخل هر روش سریال):

| سناریو | برآورد ARIMA |
|---|---|
| YouTube ساعتی (~۱۵۰۰ آیتم × ۶۵۲ پنجره) | **حدود ۶–۸ ساعت** (یک شب) |
| تاکسی ساعتی (~۲۶۰ منطقه × ۷۹۸۳ پنجره) | حدود ۱۲–۱۵ ساعت |
| تاکسی ۳۰ دقیقه | ~۲۸ ساعت — توصیه نمی‌شود |
| تاکسی ۵ دقیقه | غیرعملی (~۷ روز) — **اجرا نکنید** |

**توصیه:** ARIMA فقط روی YouTube و تاکسی ساعتی (دو بستر نماینده: انفجاری + تناوبی)؛ در متن رساله ذکر کنید که به دلیل هزینه محاسباتی ARMA مقایسه روی دو بستر انجام شد — این جمله خودش شاهد تجربی سنگینی ARMA و مکمل جدول ۴-۸ است. ARYW (نسخه سریع خانواده AR) را در هر چهار سناریو بگذارید تا پوشش کامل باشد. ضمناً زمان اجرای ثبت‌شده در `metadata\runtime_stats` هر روش را نگه دارید: «زمان واقعی ARIMA در برابر WSPI» یک عدد طلایی برای دفاع است.

---

## چک‌لیست صحت بعد از اجرا

1. در لاگ شروع، روش‌های کش‌شده در `already done` و فقط جدیدها در `to run now` باشند.
2. ستون `windows` روش‌های جدید در `main_summary.csv` با روش‌های کش‌شده برابر باشد (هم‌ترازی پنجره‌ها).
3. `Persistence` باید اعدادی نزدیک/کمی بدتر از `EWMA` بدهد — چک عقلانیت.
4. انتظار علمی: ARIMA/ARYW در Spearman رقابتی، در RSI و ΔRank ضعیف‌تر از WSPI — همان روایت مرکزی رساله. هر دو حالتِ نتیجه به نفع شماست.
