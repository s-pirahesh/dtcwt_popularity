# پیش‌بینی محبوبیت محتوا با استفاده از تجزیه فرکانسی چند مقیاسه

یک چارچوب جامع برای سنجش و پیش‌بینی محبوبیت محتوا در سیستم‌های توزیع شده با استفاده از پردازش سیگنال مبتنی بر موجک و تحلیل گراف.

## معرفی

این پروژه روش‌های پیشرفته سنجش محبوبیت را با ترکیب موارد زیر پیاده‌سازی می‌کند:
- **تبدیلات موجک**: DWT و Dual-Tree Complex Wavelet Transform (DTCWT)
- **پردازش سیگنال روی گراف**: گراف‌های رابطه محتوا با تحلیل طیفی
- **روش‌های آماری**: ARMA، EWMA و سیاست‌های سنتی کَشینگ (LRU، LFU)
- **رویکردهای ترکیبی**: ترکیب چند روش برای پیش‌بینی بهبود یافته

## ویژگی‌ها

- **پشتیبانی از چند دیتاست**: MovieLens، Youku، YouTube
- **انعطاف‌پذیری زمانی**: روزانه، ساعتی، دقیقه‌ای یا بازه‌های سفارشی
- **دو حالت ارزیابی**:
  - استاندارد: ارزیابی کامل در حافظه
  - افزایشی: ارزیابی کارآمد از نظر حافظه، ایمن در برابر خرابی
- **معیارهای جامع**: همبستگی Spearman، Kendall tau، تحلیل طبقه‌بندی شده
- **پردازش موازی**: پشتیبانی از چند هسته برای ارزیابی سریع‌تر
- **نمایش بصری غنی**: نمودارهای مقایسه عملکرد و آمارها

## نصب و راه‌اندازی

### پیش‌نیازها

```bash
Python 3.8+
pip install -r requirements.txt
```

### وابستگی‌ها

```bash
pip install numpy pandas scipy scikit-learn
pip install pywt dtcwt  # تبدیلات موجک
pip install tqdm  # نوار پیشرفت
pip install matplotlib seaborn  # نمایش بصری
```

## شروع سریع

### 1. آماده‌سازی داده

تبدیل دیتاست MovieLens:

```bash
python data/convert_movielens.py \
  --input data/raw/ml-25m \
  --output data/processed/movielens_daily.csv \
  --granularity daily
```

### 2. اجرای ارزیابی

حالت استاندارد (همه روش‌ها):

```bash
python experiments/run_popularity_assessment.py movielens \
  --num-items 500 \
  --start-date 2023-06-01 \
  --end-date 2023-08-31
```

حالت افزایشی (کارآمد از نظر حافظه):

```bash
python experiments/run_popularity_assessment.py movielens \
  --num-items 500 \
  --start-date 2023-06-01 \
  --end-date 2023-08-31 \
  --incremental
```

### 3. تحلیل نتایج

```bash
python experiments/analyze_results.py results/movielens/w30_h7_n500_top_20260207_140000
```

## آماده‌سازی داده

### دیتاست MovieLens

تبدیل دیتاست MovieLens 25M به فرمت زمانی:

```bash
python data/convert_movielens.py \
  --input data/raw/ml-25m \
  --output data/processed/movielens_daily.csv \
  --granularity daily
```

**گزینه‌های Granularity:**
- `daily`: یک رکورد در روز (پیش‌فرض)
- `hourly`: یک رکورد در ساعت
- `weekly`: یک رکورد در هفته

### دیتاست Youku

تبدیل دیتاست ویدیوی Youku:

```bash
python data/convert_youku.py \
  --input data/raw/youku \
  --output data/processed/youku_5min.csv \
  --granularity 5min
```

**گزینه‌های Granularity:**
- `5min`: بازه‌های 5 دقیقه‌ای
- `15min`: بازه‌های 15 دقیقه‌ای
- `hourly`: تجمیع ساعتی

### دیتاست سفارشی

دیتاست شما باید این ستون‌ها را داشته باشد:
- `item_id`: شناسه محتوا (عدد صحیح یا رشته)
- `timestamp`: تاریخ/زمان (datetime64[ns])
- `count`: تعداد دسترسی‌ها (عدد صحیح)

نمونه فرمت:
```csv
item_id,timestamp,count
1,2023-06-01 00:00:00,45
1,2023-06-02 00:00:00,52
2,2023-06-01 00:00:00,23
```

## ارزیابی

### حالت استاندارد

ارزیابی کامل در حافظه با همه امکانات:

```bash
python experiments/run_popularity_assessment.py DATASET [OPTIONS]
```

**آرگومان‌ها:**

- `DATASET`: نام دیتاست (`movielens`، `youtube07`، `youku`)
- `--num-items N`: تعداد آیتم‌ها برای ارزیابی (پیش‌فرض: همه)
- `--start-date YYYY-MM-DD`: تاریخ شروع ارزیابی
- `--end-date YYYY-MM-DD`: تاریخ پایان ارزیابی
- `--window-size N`: اندازه پنجره آموزش به واحد زمانی (پیش‌فرض: 30)
- `--horizon N`: افق پیش‌بینی (قدیمی، در حالت assessment استفاده نمی‌شود)
- `--methods M1 M2 ...`: روش‌های خاص برای ارزیابی (پیش‌فرض: همه)
- `--item-selection {top,random,stratified}`: استراتژی انتخاب آیتم (پیش‌فرض: top)
- `--cores N`: تعداد هسته‌های CPU برای استفاده (پیش‌فرض: همه)
- `--format {csv,parquet}`: فرمت خروجی (پیش‌فرض: csv)
- `--data-path PATH`: مسیر فایل داده سفارشی
- `--quiet`: حذف خروجی‌های جزئیات

**روش‌های موجود:**
- `AF`: فرکانس دسترسی (پایه)
- `LRU`: کمترین استفاده اخیر
- `LFU`: کمترین فرکانس استفاده
- `EWMA`: میانگین متحرک موزون نمایی
- `Statistical`: مدل آماری مبتنی بر ARMA
- `DWT+AF`: تبدیل موجک گسسته + فرکانس دسترسی
- `DTCWT+AF`: تبدیل موجک پیچیده درخت دوگانه + فرکانس دسترسی
- `Hybrid V3.0`: ترکیبی موجک-آماری
- `Hybrid V3.1`: ترکیبی پیشرفته با ویژگی‌های گراف

**مثال‌ها:**

ارزیابی 1000 آیتم برتر برای 3 ماه:
```bash
python experiments/run_popularity_assessment.py movielens \
  --num-items 1000 \
  --start-date 2023-06-01 \
  --end-date 2023-08-31
```

ارزیابی فقط روش‌های خاص:
```bash
python experiments/run_popularity_assessment.py movielens \
  --num-items 500 \
  --methods AF LRU LFU DTCWT+AF \
  --cores 4
```

نمونه‌گیری تصادفی با بازه زمانی سفارشی:
```bash
python experiments/run_popularity_assessment.py movielens \
  --num-items 200 \
  --item-selection random \
  --start-date 2023-07-01 \
  --end-date 2023-07-31
```

### حالت افزایشی

ارزیابی کارآمد از نظر حافظه با ذخیره‌سازی مداوم:

```bash
python experiments/run_popularity_assessment.py DATASET --incremental [OPTIONS]
```

**مزایا:**
- مصرف حافظه کم (<200 مگابایت)
- ایمن در برابر خرابی: نتایج به طور مداوم در پایگاه داده SQLite ذخیره می‌شوند
- پیگیری پیشرفت: ادامه از آخرین وضعیت ذخیره شده
- مناسب برای ارزیابی‌های مقیاس بزرگ

**مثال:**

```bash
python experiments/run_popularity_assessment.py movielens \
  --num-items 5000 \
  --start-date 2023-01-01 \
  --end-date 2023-12-31 \
  --incremental \
  --cores 8
```

### دانه‌بندی زمانی

چارچوب به طور خودکار دانه‌بندی‌های زمانی مختلف را مدیریت می‌کند:

**روزانه (MovieLens):**
```bash
# اندازه پنجره = 30 روز، 92 بازه زمانی را برای یک دوره 92 روزه ارزیابی می‌کند
python experiments/run_popularity_assessment.py movielens \
  --window-size 30
```

**5 دقیقه‌ای (Youku):**
```bash
# اندازه پنجره = 288 بازه (24 ساعت)، محبوبیت سطح ویدیو را ارزیابی می‌کند
python experiments/run_popularity_assessment.py youku \
  --window-size 288
```

**هفتگی:**
```bash
# ابتدا داده را به هفتگی تبدیل کنید، سپس اندازه پنجره = 8 هفته
python data/convert_movielens.py --granularity weekly
python experiments/run_popularity_assessment.py movielens \
  --window-size 8
```

## تحلیل

### تحلیل نتایج

پس از ارزیابی، نتایج را با استفاده از این دستور تحلیل کنید:

```bash
python experiments/analyze_results.py RESULTS_PATH [OPTIONS]
```

**آرگومان‌ها:**
- `RESULTS_PATH`: مسیر دایرکتوری نتایج
- `--output DIR`: دایرکتوری خروجی برای تحلیل (پیش‌فرض: همان نتایج)
- `--format {text,html,both}`: فرمت گزارش (پیش‌فرض: both)
- `--plot`: تولید نمودارهای مقایسه
- `--export-csv`: صادرات خلاصه به CSV

**مثال‌ها:**

تحلیل پایه:
```bash
python experiments/analyze_results.py results/movielens/w30_h7_n500_top_20260207_140000
```

تحلیل کامل با نمودارها:
```bash
python experiments/analyze_results.py results/movielens/w30_h7_n500_top_20260207_140000 \
  --plot \
  --format both \
  --export-csv
```

### مقایسه چند آزمایش

مقایسه نتایج از اجراهای مختلف:

```bash
python experiments/compare_experiments.py \
  results/movielens/experiment1 \
  results/movielens/experiment2 \
  results/movielens/experiment3 \
  --output comparison_report.html
```

### نمایش بصری

تولید نمودارهای نمایش بصری:

```bash
python experiments/visualize_results.py RESULTS_PATH [OPTIONS]
```

**نمودارهای موجود:**
- مقایسه عملکرد (همبستگی Spearman بر اساس روش)
- تحلیل طبقه‌بندی شده (عملکرد در هر طبقه محبوبیت محتوا)
- تحلیل زمانی (عملکرد در طول زمان)
- مقایسه زمان اجرا (زمان اجرا بر اساس روش)

**مثال:**

```bash
python experiments/visualize_results.py results/movielens/w30_h7_n500_top_20260207_140000 \
  --plots performance stratified temporal \
  --output figures/
```

## ساختار پروژه

```
dtcwt_popularity/
├── data/
│   ├── loaders/              # ابزارهای بارگذاری داده
│   │   ├── movielens_loader.py
│   │   ├── youku_loader.py
│   │   └── base_loader.py
│   ├── convert_movielens.py # اسکریپت تبدیل MovieLens
│   ├── convert_youku.py     # اسکریپت تبدیل Youku
│   └── raw/                 # دیتاست‌های خام (شامل نمی‌شود)
│
├── evaluation/
│   ├── temporal_evaluator.py      # موتور ارزیابی استاندارد
│   ├── incremental_evaluator.py  # موتور ارزیابی افزایشی
│   ├── evaluation_config.py      # کلاس‌های پیکربندی
│   ├── metrics.py                 # معیارهای ارزیابی
│   ├── stratification.py          # سیستم طبقه‌بندی
│   ├── time_utils.py              # ابزارهای بازه زمانی
│   └── method_configs.py          # پیکربندی‌های خاص روش
│
├── methods/
│   ├── base_method.py           # روش پایه سنجش
│   ├── dtcwt_assessment.py      # روش مبتنی بر DTCWT
│   ├── dwt_assessment.py        # روش مبتنی بر DWT
│   ├── statistical_assessment.py # روش‌های آماری
│   ├── hybrid_assessment.py     # روش‌های ترکیبی
│   └── baselines/
│       └── __init__.py          # روش‌های پایه سنتی (LRU، LFU و غیره)
│
├── experiments/
│   ├── run_popularity_assessment.py  # اسکریپت اصلی ارزیابی
│   ├── analyze_results.py            # تحلیل نتایج
│   ├── compare_experiments.py        # مقایسه چند آزمایش
│   └── visualize_results.py          # نمایش بصری
│
├── results/                    # نتایج ارزیابی (خودکار تولید می‌شود)
│
├── README.md                   # راهنمای انگلیسی
├── راهنما.md                   # این فایل
└── requirements.txt            # وابستگی‌های Python
```

## ساختار خروجی

### حالت استاندارد

```
results/movielens/w30_h7_n500_top_20260207_140000/
├── config.json              # پیکربندی آزمایش
├── thresholds.json          # آستانه‌های طبقه‌بندی
├── runtime_stats.json       # آمار زمان اجرا
├── metadata/
│   ├── config.json
│   ├── thresholds.json
│   └── run_metadata.json
└── results/
    ├── AF_results.csv
    ├── LRU_results.csv
    ├── DTCWT+AF_results.csv
    └── ...
```

### حالت افزایشی

```
results/w30_h7_n5000_inc_20260207_140000/
├── config.json              # پیکربندی آزمایش
├── thresholds.json          # آستانه‌های طبقه‌بندی
├── runtime_stats.json       # آمار زمان اجرا
├── metadata/
│   └── run_metadata.json
└── results.db               # پایگاه داده SQLite با همه نتایج
```

## رفع مشکلات

### مشکلات حافظه

از حالت افزایشی برای ارزیابی‌های بزرگ استفاده کنید:
```bash
python experiments/run_popularity_assessment.py movielens \
  --num-items 10000 \
  --incremental
```

### خطاهای Import

نصب وابستگی‌های گم شده:
```bash
pip install pywt dtcwt scipy
```

### مشکلات فرمت داده

اطمینان حاصل کنید داده شما ستون‌ها و انواع صحیح دارد:
```python
import pandas as pd
df = pd.read_csv('your_data.csv')
df['timestamp'] = pd.to_datetime(df['timestamp'])
df['count'] = df['count'].astype(int)
```

### عدم تطابق دانه‌بندی زمانی

اطمینان حاصل کنید دیتاست و پیکربندی مطابقت دارند:
- داده روزانه → `time_granularity='daily'`
- داده ساعتی → `time_granularity='hourly'`
- داده 5 دقیقه‌ای → `time_granularity='custom'`، `slot_duration_minutes=5`

## نکات بهبود عملکرد

1. **استفاده از پردازش موازی:**
   ```bash
   --cores 8  # استفاده از 8 هسته CPU
   ```

2. **انتخاب روش‌های کمتر:**
   ```bash
   --methods AF DTCWT+AF  # فقط روش‌های ضروری
   ```

3. **استفاده از حالت افزایشی برای دیتاست‌های بزرگ:**
   ```bash
   --incremental  # مصرف حافظه <200 مگابایت
   ```

4. **بهینه‌سازی اندازه پنجره:**
   - پنجره‌های کوچک‌تر = سریع‌تر ولی کم‌دقت‌تر
   - پنجره‌های بزرگ‌تر = کندتر ولی دقیق‌تر

## ارجاع

اگر از این چارچوب در تحقیقات خود استفاده می‌کنید، لطفاً به آن ارجاع دهید:

```bibtex
@phdthesis{popularity_wavelets_2025,
  title={سنجش و پیش‌بینی نرخ محبوبیت داده بر اساس تجزیه فرکانسی چند مقیاسه در سیستم‌های توزیع شده},
  author={سجاد},
  year={1404},
  school={دانشگاه}
}
```

## مجوز

این پروژه برای اهداف تحقیقاتی دانشگاهی است.

## تماس

برای سوالات یا مشکلات، لطفاً یک issue در GitHub باز کنید.

---

**آخرین به‌روزرسانی:** بهمن 1404  
**نسخه:** 1.0.0
