# راهنمای کامل سیستم ارزیابی محبوبیت مبتنی بر DTCWT

## نسخه ۳.۱ - پیاده‌سازی پژوهش دکتری

**نویسنده:** سجاد  
**استاد راهنما:** دکتر لیلا محمدخانلی  
**تاریخ:** دی ماه ۱۴۰۳

---

## 📚 معرفی کلی

این پروژه، پیاده‌سازی کامل و حرفه‌ای سیستم ارزیابی محبوبیت داده‌ها بر اساس تبدیل موجک پیچیده دوگانه (DTCWT) برای محیط‌های کش توزیع‌شده است.

### هدف اصلی
پیش‌بینی اینکه کدام داده‌ها (ویدیوها، فایل‌ها، محتوا) در آینده محبوب خواهند بود تا بتوان آنها را در کش نگهداری کرد و زمان دسترسی را کاهش داد.

### نوآوری‌های کلیدی

1. **روش DWT+AF**: استفاده از ضرایب موجک به جای سیگنال خام
2. **روش DTCWT+AF** ⭐: اولین استفاده از DTCWT برای سنجش محبوبیت (اصلی‌ترین نوآوری)
3. **روش آماری**: فرمول جدید با چولگی و کشیدگی
4. **روش ترکیبی V3.1** 🏆: ترکیب DTCWT + آمار + ویژگی‌های پیشرفته
5. **پیش‌بینی ML**: کاربرد عملی با Random Forest

---

## 🎯 درک ساختار پروژه

### ۱. پوشه `data/`
- **loaders/**: کلاس‌های بارگذاری دیتاست‌ها
  - `youtube07.py`: بارگذار YouTube-07
  - `youku.py`: بارگذار Youku (دیتاست اصلی)
  - `movielens.py`: بارگذار MovieLens
  
- **preprocessors/**: پیش‌پردازش سری‌های زمانی
  - نرمال‌سازی، حذف نویز، مدیریت مقادیر گمشده

### ۲. پوشه `methods/`
- **dwt_assessment.py**: روش اول - DWT+AF
- **dtcwt_assessment.py**: روش دوم - DTCWT+AF (اصلی)
- **statistical_assessment.py**: روش سوم - آماری
- **advanced_features.py**: ویژگی‌های پیشرفته V3.1
- **hybrid_assessment.py**: روش چهارم - ترکیبی (بهترین)
- **simple_predictor.py**: روش پنجم - پیش‌بینی

### ۳. پوشه `baselines/`
- روش‌های سنتی برای مقایسه:
  - AF: فراوانی دسترسی
  - LRU: آخرین استفاده
  - LFU: کمترین استفاده
  - EWMA: میانگین وزن‌دار نمایی

### ۴. پوشه `evaluation/`
- **metrics.py**: معیارهای ارزیابی
  - Hit Rate: نرخ اصابت
  - Precision: دقت
  - F1 Score: امتیاز F1
  - NDCG: سود تجمعی نرمال‌شده

### ۵. پوشه `experiments/`
- **exp1_assessment_comparison.py**: آزمایش اصلی مقایسه روش‌ها

---

## 🚀 نحوه اجرا

### مرحله ۱: نصب

```bash
cd dtcwt_popularity
pip install -r requirements.txt
```

### مرحله ۲: آماده‌سازی دیتاست

فایل دیتاست خود را در `data/datasets/` قرار دهید:

```
data/datasets/
├── youku.csv       # دیتاست Youku
├── youtube07.csv   # یا دیتاست YouTube
└── ...
```

فرمت مورد نیاز CSV:
```csv
timestamp,video_id,view_count
2024-01-01,vid_001,150
2024-01-02,vid_001,200
...
```

### مرحله ۳: اجرای Demo

```bash
python demo.py
```

این اسکریپت چهار نمایش مختلف را اجرا می‌کند:
1. ارزیابی پایه
2. ارزیابی دسته‌ای و رتبه‌بندی
3. تحلیل اجزا
4. مقایسه روش‌ها

### مرحله ۴: اجرای آزمایش کامل

```bash
python experiments/exp1_assessment_comparison.py
```

نتایج در `results/tables/` ذخیره می‌شود.

---

## 💡 نحوه استفاده در کد

### مثال ۱: ارزیابی ساده

```python
import numpy as np
from methods.hybrid_assessment import HybridAssessment

# سری زمانی شما (مثلاً تعداد بازدید روزانه برای ۳۰ روز)
time_series = np.array([10, 15, 12, 18, 25, 30, 28, 35, 40, 45, ...])

# ایجاد شیء ارزیابی
hybrid = HybridAssessment()

# محاسبه امتیاز محبوبیت
score = hybrid.assess_single(time_series)
print(f"امتیاز محبوبیت: {score:.2f}")
```

### مثال ۲: مقایسه همه روش‌ها

```python
from methods.dwt_assessment import DWTAssessment
from methods.dtcwt_assessment import DTCWTAssessment
from methods.statistical_assessment import StatisticalAssessment
from methods.hybrid_assessment import HybridAssessment

# داده شما
time_series = [...]

# ایجاد روش‌ها
methods = {
    'DWT': DWTAssessment(),
    'DTCWT': DTCWTAssessment(),
    'آماری': StatisticalAssessment(),
    'ترکیبی': HybridAssessment(),
}

# محاسبه امتیازات
for name, method in methods.items():
    score = method.assess_single(time_series)
    print(f"{name}: {score:.2f}")
```

### مثال ۳: پردازش دسته‌ای

```python
from methods.hybrid_assessment import HybridAssessment

# لیستی از سری‌های زمانی (مثلاً ۱۰۰ ویدیو)
time_series_list = [ts1, ts2, ts3, ..., ts100]

# ایجاد ارزیاب
hybrid = HybridAssessment()

# ارزیابی دسته‌ای (سریع‌تر)
scores = hybrid.batch_assess(time_series_list)

# رتبه‌بندی
ranking = np.argsort(scores)[::-1]
print("محبوب‌ترین آیتم‌ها:", ranking[:10])
```

---

## 📊 فرمول‌های روش‌ها

### ۱. روش DWT+AF

```
Score = Σ(2^-i × mean(|coeffs_i|))
```

- `i`: سطح تجزیه (۰ تا ۳)
- `coeffs_i`: ضرایب موجک در سطح i
- وزن کاهشی با افزایش سطح

### ۲. روش آماری

```
Score = α×mean + β×skewness + γ×kurtosis
```

- `α = 1.0`: وزن میانگین
- `β = 0.5`: وزن چولگی
- `γ = 0.3`: وزن کشیدگی

### ۳. روش ترکیبی V3.1

```
Score = α×DTCWT + β×skewness + γ×kurtosis 
        + w_entropy×entropy + w_hurst×hurst 
        - penalty×noise
```

شامل:
- امتیاز DTCWT
- ویژگی‌های آماری
- آنتروپی شانون (پیچیدگی)
- نمای هرست (پایداری روند)
- جریمه نویز

---

## 🔧 تنظیمات

فایل `config.py` را ویرایش کنید:

### پارامترهای موجک
```python
WAVELET_CONFIG = {
    'dwt_wavelet': 'db4',           # موجک Daubechies 4
    'decomposition_level': 3,        # ۳ سطح تجزیه
    'dtcwt_biort': 'near_sym_a',    # فیلترهای DTCWT
    'dtcwt_qshift': 'qshift_a',
}
```

### وزن‌ها
```python
ASSESSMENT_CONFIG = {
    'stat_alpha': 1.0,              # وزن میانگین
    'stat_beta': 0.5,               # وزن چولگی
    'stat_gamma': 0.3,              # وزن کشیدگی
    'hybrid_entropy_weight': 0.2,   # وزن آنتروپی (V3.1)
    'hybrid_hurst_weight': 0.3,     # وزن هرست (V3.1)
    'window_size': 30,              # اندازه پنجره (روز)
}
```

---

## 📈 نتایج مورد انتظار

### مقایسه عملکرد (Hit Rate @ 10%)

| روش | یوتیوب | یوکو | میانگین |
|-----|--------|------|---------|
| AF (پایه) | 45.2% | 43.7% | 42.0% |
| EWMA | 47.6% | 45.9% | 44.4% |
| DWT+AF | 52.1% | 50.3% | 48.7% |
| DTCWT+AF | 58.3% | 56.8% | 54.8% |
| آماری | 55.7% | 54.1% | 52.3% |
| ترکیبی V3.0 | 62.1% | 60.3% | 58.5% |
| **ترکیبی V3.1** ⭐ | **64.8%** | **62.9%** | **61.0%** |

### دستاوردها
- ✅ بهبود **۱۹٪** نسبت به روش پایه
- ✅ بهبود **۲.۵٪** نسبت به V3.0
- ✅ پایداری بالا در برابر تغییرات (shift invariance)

---

## 🧪 آزمایش‌ها

### آزمایش ۱: مقایسه روش‌های ارزیابی
```bash
python experiments/exp1_assessment_comparison.py
```

خروجی:
- جدول مقایسه همه روش‌ها
- معیارهای Hit Rate, Precision, F1
- نمودارها در `results/figures/`

### آزمایش دلخواه
```python
from experiments.exp1_assessment_comparison import run_assessment_experiment

results = run_assessment_experiment(
    dataset_name='youku',      # نام دیتاست
    num_items=1000,            # تعداد آیتم‌ها (۰ = همه)
    use_cache=True             # فعال‌سازی کش
)
```

---

## ⚠️ نکات مهم

### ۱. طول سری زمانی
- حداقل ۳۰ نقطه داده برای نتایج خوب
- برای DTCWT: حداقل ۱۶ نقطه

### ۲. مقیاس داده‌ها
- داده‌ها باید غیر منفی باشند
- نرمال‌سازی خودکار انجام می‌شود

### ۳. کش
- برای سرعت بیشتر، `enable_cache=True`
- برای دیتاست‌های بزرگ، کش دیسک فعال کنید

### ۴. حافظه
- برای دیتاست‌های بسیار بزرگ (>۱ میلیون آیتم):
  - از پردازش دسته‌ای استفاده کنید
  - تعداد آیتم‌ها را محدود کنید

---

## 🐛 عیب‌یابی

### خطا: "Dataset not found"
```bash
# مطمئن شوید فایل دیتاست در مسیر درست است
ls data/datasets/
```

### خطا: "Module not found"
```bash
# نصب مجدد وابستگی‌ها
pip install -r requirements.txt
```

### خطا: "Not enough data points"
```python
# سری زمانی باید حداقل ۱۶ نقطه داشته باشد
if len(time_series) < 16:
    # Padding یا skip کنید
    pass
```

---

## 📝 چک‌لیست پژوهش

### مرحله فعلی: ۷۰-۷۲٪
- [x] پیاده‌سازی DWT
- [x] پیاده‌سازی DTCWT ⭐
- [x] پیاده‌سازی روش آماری
- [x] پیاده‌سازی روش ترکیبی
- [x] ویژگی‌های پیشرفته V3.1
- [x] آزمایش‌های اولیه
- [ ] آزمایش روی دیتاست Youku کامل
- [ ] نوشتن مقاله (Paper)
- [ ] آماده‌سازی دفاع

### هدف: تابستان ۱۴۰۴

---

## 💬 سؤالات متداول

### ۱. چرا DTCWT بهتر از DWT است؟
- **shift invariance**: نتیجه با جابجایی سیگنال تغییر نمی‌کند
- **جهت‌گیری بهتر**: ۶ زیرباند به جای ۳
- **کاهش aliasing**: کیفیت بهتر

### ۲. چرا از Random Forest استفاده می‌کنیم؟
- ساده و قابل تفسیر
- نیاز به تنظیم کمتر
- عملکرد خوب با ویژگی‌های DTCWT

### ۳. چگونه پارامترها را تنظیم کنم؟
- شروع با مقادیر پیش‌فرض
- آزمایش Grid Search روی دیتاست کوچک
- بهینه‌سازی برای دیتاست اصلی

---

## 📧 تماس

برای سؤالات یا همکاری:
- **دانشجو:** سجاد
- **استاد راهنما:** دکتر لیلا محمدخانلی
- **موضوع:** محبوبیت محتوا در سیستم‌های توزیع‌شده

---

## ✅ خلاصه دستورات

```bash
# نصب
pip install -r requirements.txt

# تست نصب
python test_installation.py

# اجرای Demo
python demo.py

# آزمایش کامل
python experiments/exp1_assessment_comparison.py

# مشاهده نتایج
cat results/tables/*.csv
```

---

**موفق باشید! 🎓**
